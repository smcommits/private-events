<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
      <div class="container-fluid">
        <ul class="navbar-nav">
           <li class="nav-item"><%= link_to "Create Event", new_event_path, class: "nav-link"%></li>
        </ul>

        <div class="mx-auto order-0">
          <%= link_to "Private Events", root_path, class: "navbar-brand"%>
        </div>

       <div class="navbar-nav">
         <ul class="navbar-nav ml-auto">
           <%unless logged_in? %>
             <li class="nav-item"><%= link_to "Login", login_path, class: "nav-link"%></li>
             <li class="nav-item"><%= link_to "Sign Up", new_user_path, class: "nav-link" %></li>
           <%end%>
           <% if logged_in? %>
             <li class="nav-item"><%= link_to "Profile", user_path(session[:current_user_id]), class: "nav-link"%></li>
             <li class="nav-item"><%= link_to "Log Out", logout_path, method: :delete, class: "nav-link"%></li>
           <%end%>
         </ul>
       </div> 
         
        
         </ul>
       </div>

      </div>
    </nav>
    <%= yield %>
  </body>
</html>
