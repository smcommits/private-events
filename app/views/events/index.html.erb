<h2>All Events</h2>

<% @events.each do |event| %>
  <article>
    <h3><%= event.title %></h3>
    <small> <%= "Create by: #{event.creator.username}"%> </small>
    <small> <%= "Event Date: #{event.event_date}" %></small>
    <p> <%= event.description %>
  </article>
<% end%>
